# Test merge log · 2026-07-14

## 범위

- 마이 프로필 편집 경험을 정리했다.
- 다른 사용자 프로필 레이아웃을 정리했다.
- 팔로잉·팔로워 목록 밀도를 높였다.
- 채팅 빈 상태와 채팅방 나가기를 정리했다.
- 알림 시간과 삭제 동작을 개선했다.
- 현재 음악과 인기 음악 커버 흐름을 연결했다.
- 아티스트 이미지 공급원을 추가했다.
- DiceBear 아바타 표시를 모든 사용자 화면에 통일했다.
- 사용하지 않는 멜로디 별칭 기능을 제거했다.
- 온보딩과 프로필 장르 목록을 원격 카탈로그로 전환했다.

## 프로필

- 마이 탭 상단의 중복 편집 버튼을 제거했다.
- 프로필 카드 자체를 기본 프로필 편집 진입점으로 유지했다.
- 기본 프로필 편집을 중앙 대형 다이얼로그로 표시한다.
- 프로필 테마 선택 UI를 제거했다.
- 기존 프로필 색상은 호환 목적으로 그대로 보존한다.
- 새 아바타 뽑기에 즉시 미리보기를 적용했다.
- 아바타 저장 실패 시 이전 이미지로 복원한다.
- 대표곡과 최애 아티스트를 공개 프로필과 같은 레이아웃으로 표시한다.
- 대표곡의 순위 숫자와 외곽 테두리를 제거했다.
- 공개 프로필의 전체 보기 위치를 공통 취향 아래로 조정했다.
- 차단·신고 영역 높이를 줄였다.
- 다른 사용자 상세 화면에서 현재 음악 카드를 제거했다.
- 공개 프로필에서는 현재 음악 카드를 유지한다.
- 프로필 액션은 작은 리액션·팔로우 액션으로 정리했다.

## 음악 편집

- iTunes 검색 요청 한도를 30개로 늘렸다.
- 대표곡 결과만 독립적으로 스크롤된다.
- 아티스트 결과만 독립적으로 스크롤된다.
- 검색창은 결과 스크롤 밖에 고정된다.
- 저장 버튼은 결과 스크롤 밖에 고정된다.
- 선택한 대표곡 요약은 검색창 위에 유지된다.
- 선택한 아티스트 요약은 검색창 위에 유지된다.
- 대표곡은 최대 3곡 제한을 유지한다.
- 최애 아티스트는 최대 3명 제한을 유지한다.
- 검색 결과 30번째 항목까지 접근하는 UI 테스트를 추가했다.

## 아티스트 이미지

- Deezer 아티스트 검색 API를 추가했다.
- 호출 주소는 `https://api.deezer.com/search/artist?q={아티스트명}`이다.
- 아티스트 이름은 공백·기호·대소문자를 정규화해 비교한다.
- 정확히 일치하는 아티스트의 `picture_small`을 우선 사용한다.
- Deezer 이미지가 없으면 iTunes 앨범 커버를 사용한다.
- 앨범 커버도 없으면 음악 아이콘을 표시한다.
- 아티스트 이니셜 대체 표시는 사용하지 않는다.
- 온보딩 아티스트 선택에도 같은 규칙을 적용한다.
- 프로필 편집 아티스트 선택에도 같은 규칙을 적용한다.

## 앨범 커버

- Android 미디어 메타데이터의 HTTPS 커버 URL을 우선 사용한다.
- 커버 URL이 없으면 곡 제목과 아티스트로 iTunes를 검색한다.
- 보완한 커버 URL을 현재 재생 상태에 반영한다.
- 보완한 커버 URL을 주변 음악 상태와 함께 서버에 동기화한다.
- 주변 인기 음악 응답에 커버 URL을 추가했다.
- 주변 음악 실시간 이벤트에 커버 URL을 추가했다.
- 상대 프로필 현재 음악 응답의 커버 URL을 앱 모델에 연결했다.
- 서버 응답에 커버가 없을 때 앱에서 한 번 더 보완한다.
- 홈 인기 음악 카드에서 커버를 렌더링한다.
- 상대 프로필 지금 듣는 음악 카드에서 커버를 렌더링한다.
- 미리듣기 바에도 동일한 커버 URL을 전달한다.

## 아바타

- DiceBear Thumbs URL 생성 규칙을 한 곳으로 모았다.
- 서버 아바타 URL이 있으면 그대로 사용한다.
- URL이 없으면 서버의 아바타 시드를 사용한다.
- 시드도 없으면 안정적인 프로필 식별자를 사용한다.
- 마이 프로필에 동일한 해석 규칙을 적용한다.
- 공개 프로필에 동일한 해석 규칙을 적용한다.
- 팔로잉 목록에 동일한 해석 규칙을 적용한다.
- 팔로워 목록에 동일한 해석 규칙을 적용한다.
- 근처 사용자 버블에 동일한 해석 규칙을 적용한다.
- 근처 사용자 상세 진입 전 카드에도 실제 아바타를 표시한다.
- 아바타가 없는 오래된 사용자도 이니셜 대신 DiceBear를 표시한다.

## 장르

- 하드코딩 장르 배열을 제거했다.
- iTunes 음악 장르 카탈로그를 불러온다.
- 온보딩과 프로필 편집이 같은 장르 상태를 공유한다.
- 장르 로딩 상태를 표시한다.
- 장르 요청 실패 메시지를 표시한다.
- 장르 요청 재시도 버튼을 제공한다.
- 사용자가 이미 선택한 장르는 응답과 합쳐 유지한다.
- 장르는 최대 6개 선택 제한을 유지한다.

## 알림

- 알림 수신 시 서버 생성 시각을 보존한다.
- 서버 ISO-8601 시각의 밀리초·나노초 형식을 처리한다.
- 1시간 미만은 `최근`으로 표시한다.
- 1시간 이상은 `n시간 전`으로 표시한다.
- 24시간 이상은 `n일 전`으로 표시한다.
- 저장된 알림도 화면을 다시 열 때 현재 시각으로 계산한다.
- 모두 지우기는 항목이 순서대로 빠져나가는 애니메이션을 사용한다.
- 개별 스와이프 삭제도 퇴장 애니메이션 뒤에 저장소를 갱신한다.
- 삭제 중 중복 탭을 막는다.

## 채팅

- 실제 메시지가 있는 채팅만 기본 목록에 표시한다.
- 채팅이 없을 때 안내 문구와 시작 버튼을 표시한다.
- 시작 버튼에서 맞팔 사용자를 선택할 수 있다.
- 채팅방 나가기는 알림 삭제와 같은 왼쪽 스와이프를 사용한다.
- 나간 채팅방은 기기 계정별 숨김 상태로 보존한다.
- 새 메시지가 오면 숨김 상태를 해제한다.
- 다시 채팅을 열면 숨김 상태를 해제한다.
- 스와이프 확정 후 삭제 배경만 남는 상태를 막았다.
- 빈 채팅 스와이프 회귀 테스트를 추가했다.

## 멜로디 별칭

- 마이 탭의 멜로디 별칭 진입점을 제거했다.
- 멜로디 별칭 화면을 제거했다.
- AI 생성 상태와 ViewModel 동작을 제거했다.
- 앱의 OpenAI 멜로디 API 클라이언트를 제거했다.
- 멜로디 미리듣기 플레이어를 제거했다.
- 서버의 OpenAI 멜로디 생성 컨트롤러를 제거했다.
- 서버 OpenAI 설정을 제거했다.
- 프로필의 멜로디 별칭 수정 엔드포인트를 제거했다.
- 새 오프라인 교환 카드에는 별칭을 빈 값으로 보낸다.
- 기존 DB·교환 서명 호환 필드는 읽기 호환 목적으로 유지한다.
- 버블 모드 카드와 교환 기록 UI에는 별칭을 표시하지 않는다.

## 검증

- Android debug Kotlin 컴파일을 통과했다.
- Android 단위 테스트를 통과했다.
- Android instrumentation Kotlin 컴파일을 통과했다.
- Android lint를 통과했다.
- 서버 Kotlin 컴파일을 통과했다.
- 서버 단위 테스트를 통과했다.
- 실제 SM-G970N 기기에서 instrumentation 테스트를 통과했다.
- 물리 기기 전용 2대 교환 시나리오는 환경 조건에 따라 건너뛴다.
- 테스트 뒤 최신 debug APK를 다시 설치한다.
- 설치 뒤 메인 액티비티 실행과 치명적 크래시 로그를 확인한다.
